# Repository Instructions

- Do not run `git commit` or `git push` unless the user explicitly asks for it in the current chat.
- Leave website code changes uncommitted by default so GitHub Desktop shows the changed files and the user can press the commit button manually.
- If the user asks to make the site live, confirm whether they want a commit and push before doing any git action.
